import SwiftUI

struct ItemListView: View {
    // TODO: Day 9 — 时间线物品列表
    var body: some View {
        NavigationStack {
            Text("物品列表")
                .navigationTitle("列表")
        }
    }
}
